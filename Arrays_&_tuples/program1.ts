let arr = [1, 2, 4];
console.log(arr);

function printNumbers(nums: number[]) {
    console.log(nums);
}

printNumbers([1, 2, 4])

// Tuple
function printTuple(nums: [number, number, string]) {
    console.log(nums);
}

printTuple([1, 1, 'Ajay']);
